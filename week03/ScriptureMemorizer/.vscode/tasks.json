{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/ScriptureMemorizer.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}
//I used chat GPT to make the enter work, VS code gave me errors